import React from "react";
import PropTypes from "prop-types";

const CreatePetProfile = (props) => {
  return (
    <div>user, name, age, breed, gender, characteristics, description, location, lookingFor(location, breed, gender, age, whatfor, desription), social(youtube, twitter, facebook, instagram)</div>
  );
};

CreatePetProfile.propTypes = {};

export default CreatePetProfile;
