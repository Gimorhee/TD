import React from "react";
import PropTypes from "prop-types";

const UserPetProfile = ({ user, profile }) => {
  return <section className="userPetProfile"></section>;
};

UserPetProfile.propTypes = {};

export default UserPetProfile;
